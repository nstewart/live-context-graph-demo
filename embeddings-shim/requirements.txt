# Web server
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.6.0

# Local CPU vector embeddings (BAAI/bge-small-en-v1.5, 384-dim, ONNX).
# Pin range matches api/ and the former search-sync so query-time and
# index-time vectors come from the exact same model weights.
fastembed>=0.3.6
