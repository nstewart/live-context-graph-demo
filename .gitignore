# Environment
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/
.venv/
venv/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dist/
.vite/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Docker
docker-compose.override.yml

# Logs
*.log
logs/

# Database
*.db
*.sqlite

# Test
coverage/
.nyc_output/

# AWS deployment state
aws/.state/

# fastembed model cache (downloaded locally when running outside Docker)
api/.fastembed_cache/

# Claude Code scratch worktrees (transient checkouts, hundreds of MB)
.claude/worktrees/

# Installer downloads (e.g. AWSCLIV2.pkg fetched while setting up deploys)
*.pkg

# Resolved label artifacts (regenerate with `make label`).
# NOTE: web/src/generated/label.json is deliberately NOT ignored -- it is
# committed so a bare `npm run dev` / `vitest` works without Python.
labels/.resolved/
os-bootstrap/rendered/
